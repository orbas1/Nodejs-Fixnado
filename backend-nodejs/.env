NODE_ENV=development
PORT=4000

# Core database configuration for local development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fixnado_dev
DB_USER=fixnado_user
DB_PASSWORD=fixnado_password_123
DB_DIALECT=postgres

# JWT Secret for local development
JWT_SECRET=local_development_jwt_secret_key_12345

# Disable AWS Secrets Manager for local development
SECRETS_MANAGER_ENABLED=false

# Disable security audit for local development
SECURITY_AUDIT_ENABLED=false

# Disable finance alerts for local development
FINANCE_ALERTS_ENABLED=false